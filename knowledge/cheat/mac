Various defaults: https://macos-defaults.com/

# Disable press-and-hold for nvim

defaults write nvim-qt ApplePressAndHoldEnabled NO

# Stop Tunnelblick from changing hostname constantly

sudo scutil --set HostName `hostname` # (when tunnelblick is stopped)
