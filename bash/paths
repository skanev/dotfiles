if [ -d /work/runtimes/plt-scheme/bin ]; then
	# TODO Rather uncool, but (1) I'm lazy right now and (2) let's see how long
	# I can stand to have a TODO in a public repository
	PATH="$PATH:/work/runtimes/plt-scheme/bin"
fi

if [ -d /opt/local/bin ]; then
	# MacPorts
	PATH="/opt/local/bin:/opt/local/sbin:$PATH"
fi

PATH="$PATH:$HOME/bin/"
export PATH
